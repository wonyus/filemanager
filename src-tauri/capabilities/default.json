{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the primary application window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
